services:
  - type: web
    name: groq-chatbot
    env: python
    pythonVersion: 3.11
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn chatbot:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
